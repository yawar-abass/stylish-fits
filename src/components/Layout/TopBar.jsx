import React from "react";

const TopBar = () => {
  return (
    <div className="h-9 bg-black flex items-center justify-center text-white text-sm">
      <span className="font-bold">BUY ONLINE + PICK UP IN STORE </span>
      <span>- select “store pick up” at checkout + pick up same day!</span>
    </div>
  );
};

export default TopBar;
